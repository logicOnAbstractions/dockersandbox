* a simple sandbox docker project to test things out.
* contains a "default" working setup with docker running a basic python program
* we can start from that & then just test things out after copying it